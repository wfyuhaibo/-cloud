spring:
  cloud:
    nacos:
      discovery:
        server-addr: 47.103.1.53:8848 #nacos注册服务器地址
        namespace: 5b1e00c4-9053-4102-b7d5-39b0f1ca42f2 #默认为public命名空间,可以省略不写
      config:
        server-addr: 47.103.1.53:8848 #nacos配置服务器地址
        namespace: 5b1e00c4-9053-4102-b7d5-39b0f1ca42f2 #默认为public命名空间,可以省略不写
        #        group: DEFAULT_GROUP #指定配置群组 --如果是public命名空间 则可以省略群组配置
        #        prefix: providertest #文件名 -- 如果没有配置则默认为 ${spring.appliction.name}
        file-extension: yml #指定文件后缀， 默认properties
